apiVersion: v1
kind: Service
metadata:
  name: {{ include "servlet-example.fullname" . }}
  labels:
    {{- include "servlet-example.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servlet-example.selectorLabels" . | nindent 4 }}
