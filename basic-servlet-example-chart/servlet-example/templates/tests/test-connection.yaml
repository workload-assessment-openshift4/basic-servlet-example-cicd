apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servlet-example.fullname" . }}-test-connection"
  labels:
    {{- include "servlet-example.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servlet-example.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
